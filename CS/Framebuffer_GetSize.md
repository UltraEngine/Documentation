## Syntax

```csharp
Vec2 GetSize()
```

## Returns
Returns the framebuffer width and height, in display pixels.

## Example

```csharp
Vec2 size = GetSize();
int width = size.x;
int height = size.y;
```