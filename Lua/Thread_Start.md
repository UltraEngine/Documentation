# Thread:Start()

This method begins execution of a thread.

## Syntax

`Thread:Start()`

### Return Value

This method does not return any value.

## Example

```lua
-- Create a thread object
local thread = Thread()

-- Start the thread
thread:Start()
```